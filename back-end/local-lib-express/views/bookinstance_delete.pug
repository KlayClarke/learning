extends layout 

block content 
  h1 #{title}: #{bookinstance.book.title} #{bookinstance.imprint} 
  p Do you really want to delete this bookinstance?
  form(method='POST' action='')
    .form-group 
      input#bookinstanceid(type='hidden' name='bookinstanceid' required='true' value=bookinstance._id)
    button(type='submit') Delete 
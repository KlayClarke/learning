const blue = require("./blue");
const sadie = require("./sadie");

const allCats = [blue, sadie];

module.exports = allCats;

// when you attempt to require entire directory, node will look for index.js file
